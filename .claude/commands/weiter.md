# Session fortsetzen

Wie `/begin`, aber ohne Grundlagen — fuer Wiederaufnahmen am gleichen Tag.

## Vorgehen

1. `next-session-handoff.md` lesen
2. `git log --oneline -5` + `git status`
3. Kurz zusammenfassen wo wir stehen (3-5 Saetze)
4. Direkt fragen: "Womit weiter?"

Keine Memory-Session neu starten, keine Architektur-Recaps.
